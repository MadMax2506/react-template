export enum LanguageConfiguration {
  DE = 'de',
  EN = 'en',
}

export enum LanguageTag {
  DE_DE = 'de-DE',
  EN_US = 'en-US',
}

export const DEFAULT_LANGUAGE = LanguageConfiguration.DE;
export const DEFAULT_NS = 'common';
